<!DOCTYPE html>
<html>
	<head>
		<title>Jessie - Build your own Jessie</title>
		<link rel='stylesheet' type="text/css" href='css/global.css'>
	</head>
	<body>
		<div id="container">
			<h1>Jessie - Build your own Jessie</h1>
			
			<div id="builder">

				<form method="get" action="buildresponse/">

					<% for(var i = 0; i < functions.length; i++) { %>
						<div class="function">
							<h2><%=functions[i].name%></h2>
							<% for(var j = 0; j < functions[i].renditions.length; j++) { %>
								<div class="rendition">
									<input id="<%=functions[i].name+functions[i].renditions[j].id%>" type="radio" name="<%=functions[i].name%>" value="">
									<label for="<%=functions[i].name+functions[i].renditions[j].id%>"><%=functions[i].renditions[j].id%> : <%=functions[i].renditions[j].description%></label>
									<!--
									<pre><code><%=functions[i].renditions[j].getContents()%></code></pre>
									-->
								</div>
							<% } %>
						</div>
					<% } %>
					<div class="actions">
						<input type="submit" value="Download" name="download">
					</div>
				</form>
			</div>
		</div>
	</body>
</html>